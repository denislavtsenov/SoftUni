function solve(first, second, third) {

  let totalLength = first.length + second.length + third.length;

  console.log(totalLength);
  console.log(Math.round(totalLength / 3));
}

solve('chocolate', 'ice cream', 'cake');
